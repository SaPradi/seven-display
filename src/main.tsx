import React from 'react';
import ReactDOM from 'react-dom/client';
import SevenDisplay from './pages/SevenDisplay/SevenDisplay';
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SevenDisplay />
  </React.StrictMode>,
)
